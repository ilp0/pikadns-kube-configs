apiVersion: v1
kind: Service
metadata:
  name: {{ include "pikadns.fullname" . }}
  labels:
    {{- include "pikadns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "pikadns.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
